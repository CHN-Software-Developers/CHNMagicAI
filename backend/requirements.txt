# CHNMagicAI backend (installed into the vendored engine's Python env).
# aiohttp is already provided by ComfyUI; listed for standalone dev envs.
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
aiohttp>=3.9
huggingface_hub>=0.24
aiofiles>=23.2
